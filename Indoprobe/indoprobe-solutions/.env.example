# Base URL of the deployed site (used for SEO metadata, sitemap, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://www.indoprobe.in

# Note: WhatsApp number and email are configured in src/config/contact.ts,
# not via environment variables, so they can be edited directly in code.
